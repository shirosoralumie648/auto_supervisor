export type ArtifactKind = "roadmap" | "design" | "spec";

export type ReviewDecision = "pass" | "revise" | "block" | "needs_human";

export type StageStatus = "not_started" | "in_progress" | "completed" | "blocked";
